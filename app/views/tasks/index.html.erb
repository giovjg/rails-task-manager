<h1> My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%=  link_to task.title, task_path(task.id)%><input type="checkbox"></li>
  <% end %>
</ul>

<h2><%=  link_to "Add a new task", tasks_new_path(@task)%></h2>
