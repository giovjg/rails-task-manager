<h1> My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%=  link_to task.title, task_path(task.id)%><input type="checkbox"></li>
  <% end %>
</ul>

<%= link_to "Add this task", edit_task_path(@task) %>
